//! ============================================================================
//! domain — 비즈니스 모델 + 상태 보관 서비스.
//! ============================================================================
//!
//! - `model`   : 깨끗한 도메인 타입 (한국어 약어 X, 필요한 의미만 노출)
//! - `service` : 도메인 데이터를 가져오고 메모리에 보관하는 싱글톤
//!               UI 와 다른 도메인은 항상 service 를 거쳐서 데이터를 얻는다
//!               (DTO 는 `data` 레이어 안에 머물고 도메인 밖으로 새지 않음).

pub mod model;
pub mod service;
