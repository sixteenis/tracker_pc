//! ============================================================================
//! data — 외부 데이터 소스 레이어.
//! ============================================================================
//!
//! 두 종류:
//!   - `api`     : 원격 서버 통신 (HTTP)
//!   - `local`   : 로컬 SQLite (오프라인 큐잉 + 단순 캐시)
//!
//! `dto` 는 wire 포맷 그대로의 약어 필드(`mbrsid`, `cmpsid` 등) 를 들고 있는
//! 직렬화용 타입이며 **`data` 레이어 밖으로 노출하지 않는다**. 외부에서 사용할
//! 의미 있는 모델은 `domain::model` 에 따로 정의되어 있고, `repository` 가
//! DTO ↔ 도메인 모델 변환을 담당한다.

pub mod api;
pub mod dto;
pub mod local;
pub mod repository;
